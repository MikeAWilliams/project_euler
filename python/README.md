# Project Euler — Python Solutions

Each problem is in its own file (`p30.py`, `p32.py`, etc.) and can be run directly:

```
python p30.py
```

## Shared library

`euler_lib.py` contains utility functions used across multiple problems.

## Tests

```
python -m pytest
```
